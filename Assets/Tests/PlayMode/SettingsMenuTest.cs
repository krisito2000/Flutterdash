using System.Collections;
using System.Collections.Generic;
using NUnit.Framework;
using UnityEngine;
using UnityEngine.TestTools;
using UnityEngine.UI;

public class SettingsMenuTest
{
    [Test]
    public void SetFullScreenTest()
    {
        //// Arrange
        //var mainMenuObject = new GameObject();
        //var settingsMenu = mainMenuObject.AddComponent<SettingsMenu>(); // Replace 'MainMenu' with the actual class name if different.

        //var fullscreenToggleObject = new GameObject();
        //var fullscreenToggle = fullscreenToggleObject.AddComponent<Toggle>();
        //fullscreenToggle.isOn = false; // Set initial state to false.

        //// Act
        //settingsMenu.SetFullscreen(fullscreenToggle);

        //// Assert
        //Assert.AreEqual(fullscreenToggle.isOn, Screen.fullScreen);
    }
}
